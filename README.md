# vehicles_detect_count
Activate conda env variable  vehicles_detect_count

download video from the link

pip install opencv-python
pip install ultralytics
pip install cvzone

run the code anaconda prompt
python main.py
